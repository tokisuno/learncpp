#include <iostream>

using namespace std;

int main() {
    int num { };

    cout << "Enter an int: ";
    cin >> num;

    cout << "Double that number is: " << num * 2 << '\n';
    cout << "Triple that number is: " << num * 3 << '\n';

    return 0;
}
