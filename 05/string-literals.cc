#include <iostream>

int main() {
    std::cout << "Hello world!";
    // "Hello world is a string literal
    // String literals are placed between double quotes to identify them as strings
    // Strings are not a fundamental type of C++

    return 0;
}
