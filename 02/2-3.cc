#include <iostream>

using namespace std;

void printHi() {    // void doesn't return a value
    cout << "Hi\n"; // so they don't need a return statement
}

int main() {
    printHi();

    return 0;
}
